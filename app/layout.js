import "./globals.css";

export const metadata = {
  title: "Aurelia — The right stack, not just a stack",
  description:
    "Aurelia builds full-stack Next.js apps wired to whichever database actually fits the job — Postgres, MongoDB, Prisma, or Firebase.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link
          rel="preconnect"
          href="https://fonts.gstatic.com"
          crossOrigin="true"
        />
        <link
          href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600;700&family=IBM+Plex+Sans:wght@400;500;600&display=swap"
          rel="stylesheet"
        />
      </head>
      <body>{children}</body>
    </html>
  );
}
